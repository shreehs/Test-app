<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.intplus.shoppingspace">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ActivityWebView"
            android:label="@string/title_activity_activity_web_view"
            android:theme="@style/AppTheme.NoActionBar">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.intplus.shoppingspace.DashboardActivity" />
        </activity>
        <activity
            android:name=".TestActivity"
            android:label="@string/title_activity_test"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".TestGridActivity" />
        <activity
            android:name=".DashboardActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AllShopsActivity"
            android:label="@string/title_activity_all_shops"
            android:theme="@style/ShoppingSpaceDrawer" />
        <activity android:name=".HelpActivity" />
        <activity android:name=".AboutActivity" />
        <activity
            android:name=".OptionsActivity"
            android:label="Options"
            android:theme="@style/ShoppingSpaceDrawer">
        </activity>
    </application>

</manifest>